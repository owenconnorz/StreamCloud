<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- AppCompat-based theme. AppCompatActivity (used by MainActivity to satisfy
         CloudStream plugin casts) requires a Theme.AppCompat ancestor or it throws
         IllegalStateException on launch. Compose works fine with AppCompat themes. -->
    <style name="Theme.StreamCloud" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <style name="Theme.StreamCloud.Splash" parent="Theme.StreamCloud">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- AppCompat variant — now inherits from Theme.StreamCloud which is already
         AppCompat-based. Kept as a named style because AndroidManifest references it
         directly for PluginSettingsActivity. -->
    <style name="Theme.StreamCloud.AppCompat" parent="Theme.StreamCloud">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>
</resources>
